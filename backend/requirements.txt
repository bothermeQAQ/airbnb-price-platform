# --- web framework ---
fastapi==0.136.1
uvicorn==0.47.0
pydantic==2.13.4

# --- inference stack ---
# Pinned to the exact versions the model artifacts were trained/pickled with,
# so the saved .pkl files unpickle reliably on a fresh deploy.
numpy==2.4.2
pandas==3.0.0
scikit-learn==1.8.0
lightgbm==4.6.0
xgboost==3.2.0
catboost==1.2.10
