# --- web framework ---
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
pydantic>=2.7

# --- inference stack ---
# Loaded by saved_models/inference.py to unpickle and run the base models.
# When the local venv is created with --system-site-packages these resolve to
# the already-installed system packages; for a clean/Render install pip
# installs them from scratch.
numpy>=1.26
pandas>=2.0
scikit-learn>=1.4
lightgbm>=4.3
xgboost>=2.1
catboost>=1.2
